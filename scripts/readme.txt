Объект. событие

Объект. действие
------------------------------------------------
Объект. событие (функция()
{
объект. действие ;
});
--------------------------------------------------
событие = наступление интервала

объект с атрибутов невидимости делаем видимым
объект с атрибутов видимости делаем невидимы
-------------------------------------------------------------------------

setTimeout(function(){

$("div:hidden").show(); объект с атрибутов невидимости делаем видимым
$("div:visible").hide(); объект с атрибутов видимости делаем невидимы
}, 25000);